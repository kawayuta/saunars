<%= form_with(model: sauna_tag) do |form| %>
  <% if sauna_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sauna_tag.errors.count, "error") %> prohibited this sauna_tag from being saved:</h2>

      <ul>
        <% sauna_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
